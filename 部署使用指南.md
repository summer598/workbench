# WorkBuddy 手机端工作台 · 部署与使用指南（PWA版）

> 4大模块：① 全网热点 ② 黄金股市投研复盘 ③ 个人资产记账 ④ 服饰直播电商周报
>
> 本版本已升级为 **PWA 应用**：可安装为手机独立APP、离线可用、数据自动保存到本地（IndexedDB）、支持导出/导入实现多设备同步。

---

## 一、安装为手机APP（PWA方式）

### 所需文件

将以下3个文件上传到同一目录（任意静态托管服务）：
- `index.html` — 工作台主程序
- `manifest.json` — APP安装配置
- `sw.js` — Service Worker离线缓存

> 推荐：GitHub Pages、Vercel、Netlify、个人服务器，任意能HTTPS访问的静态托管即可。

### iPhone（Safari安装）

1. iPhone 用 **Safari** 打开工作台网址（必须Safari，Chrome不行）
2. 点击底部「**分享**」图标（方框+向上箭头）
3. 选择「**添加到主屏幕**」
4. 命名「我的工作台」→ 完成
5. 桌面出现独立APP图标，点开**全屏运行，无浏览器地址栏，和原生APP一样**

### Android（Chrome安装）

1. 用 **Chrome** 打开工作台网址
2. 地址栏会提示「**安装应用**」→ 点击安装
3. 或点击右上角「⋮」→「**添加到主屏幕**」→「安装」
4. 桌面生成独立APP图标，可直接从桌面启动

### 应用内安装提示

工作台打开3秒后会弹出底部安装横幅，点击「安装」直接安装。关闭后可通过浏览器菜单手动安装。

### 离线可用

安装后，Service Worker 会缓存工作台核心资源。**断网也能打开APP**，查看已保存的本地数据（记账、投资、资产记录）。热点/投研/电商等需要定时任务更新的模块，离线时展示上次缓存内容。

---

## 二、数据自动保存与同步（核心功能）

### 自动保存机制

| 数据类型 | 存储方式 | 说明 |
|---|---|---|
| 收支记录 | IndexedDB | 每次记录自动存入本地，**关APP不丢** |
| 投资交易 | IndexedDB | 同上 |
| 资产明细 | IndexedDB | 同上 |
| 同步URL设置 | localStorage | 持久保存 |

**每次记录后右上角会显示「✓ 已自动保存」提示**，数据安全不丢失。

### 多设备同步（导出/导入）

1. 点击右上角 **☁ 云同步** 按钮
2. **导出全部数据**：下载JSON备份文件，包含所有收支/投资/资产记录
3. **导入数据**：在另一台设备上选择之前导出的JSON文件，自动合并导入
4. 适合场景：手机↔平板切换、定期备份防丢失

### 可选：云端自动同步

在「☁ 数据同步」弹窗中填写「云端同步URL」后，每次新增记录会自动POST到该地址：
```
POST {你的同步URL}
Content-Type: application/json
Body: {action:"addRecord", payload:{...}, device:"dev_xxx", ts:...}
```
- 需自行搭建一个接收端（可用Vercel Serverless、Cloudflare Workers等免费方案）
- 未配置则跳过，不影响本地使用

### 导出Excel备份

资产模块底部点击「**导出Excel**」→ 下载CSV文件（Excel可直接打开），包含收支、投资、资产三个表。

### 清空数据

如需重置，点击「**清空数据**」按钮，确认后清空所有本地记录。

---

## 三、4大模块功能说明

### 模块1：全网热点（首页）

| 功能 | 说明 |
|---|---|
| 数据源 | 微博热搜、知乎热榜、财联社、新浪财经 |
| 刷新 | 定时任务每2小时更新一次（存入知识库） |
| 手动刷新 | 页面右上角「刷新」按钮 |
| 财经标注 | 财经热点标红置顶，标注利好/利空 |
| 历史检索 | 知识库按 `热点_日期` 搜索 |

### 模块2：黄金股市投研

| 子模块 | 更新时间 | 内容 |
|---|---|---|
| 每日要闻早报 | 每日 07:30 | 美联储/美元/地缘/大宗/A股政策/行业要闻 |
| 收盘市场复盘 | 工作日 18:00 | A股大盘+黄金市场+次日预判 |
| 周度汇总 | 每周日 | 5个交易日对比复盘 |

- 历史复盘检索：知识库按 `复盘_YYYY-MM-DD` 搜索
- 所有内容末尾标注「不构成投资建议」

### 模块3：个人资产记账（数据自动保存）

| 功能 | 操作 |
|---|---|
| 总资产总览 | 自动汇总所有资产项，实时计算总盈亏 |
| 资产明细 | 可添加/删除资产项（活期/黄金/股票/理财），含盈亏 |
| 日常收支记录 | 手动录入（类型/分类/金额/备注），**自动保存** |
| 投资交易记录 | 录入买卖（标的/价格/数量/逻辑），**自动保存** |
| 周开支图表 | 基于真实记录数据自动生成柱状图 |
| 记录列表 | 显示最近10条记录，可删除 |
| 导出Excel | 下载CSV（Excel可打开） |
| 全量导出/导入 | JSON格式，用于多设备同步 |
| 清空数据 | 一键重置 |

### 模块4：服饰直播电商周报

| 内容 | 频率 |
|---|---|
| 服饰大盘趋势（女装/男装） | 每周一 09:00 |
| 带货达人TOP榜 | 每周一 09:00 |
| 爆款单品排行 | 每周一 09:00 |
| 短视频脚本拆解 | 每周一 09:00 |
| 运营技巧复盘 | 每周一 09:00 |

---

## 四、自动化任务配置

详见 `自动化任务模板.md` 文件。

| 任务 | 频率 | Cron | 标签 |
|---|---|---|---|
| 全网热点聚合 | 每2小时 | `0 */2 * * *` | hot |
| 财经要闻早报 | 每日7:30 | `30 7 * * *` | morning |
| 收盘市场复盘 | 工作日18:00 | `0 18 * * 1-5` | review |
| 服饰电商周报 | 每周一9:00 | `0 9 * * 1` | ecom |

配置路径：WorkBuddy App → 自动化 → 新建定时任务

---

## 五、数据复盘与检索

### 本地数据复盘（记账/投资/资产）
- 打开APP资产模块，所有记录自动加载
- 周开支图表自动生成
- 导出Excel后可做月度/季度统计
- 全量导出JSON长期存档

### 知识库检索（热点/复盘/周报）
1. 打开 WorkBuddy App → 记忆库/知识库
2. 支持检索：
   - 按标签：`hot` / `morning` / `review` / `ecom`
   - 按日期：`2026-07-30 复盘`
   - 按关键词：`黄金 美联储`、`女装 TOP`
3. 历史报告自动归档，可回溯任意时间点

### 复盘建议
- **投资复盘**：每周日阅读「周复盘」，结合APP内个人交易记录分析决策得失
- **财务复盘**：月末导出Excel，分析月度开支结构、投资盈亏比例
- **电商学习**：每周一阅读周报，积累爆款脚本模板和运营技巧库

---

## 六、风险提示

> ⚠ **重要声明**
>
> 本工作台所有投资类内容（黄金行情、A股复盘、市场分析、次日预判等）**仅为行情资讯整理与数据分析参考，不构成任何投资理财建议**。
>
> 投资有风险，决策需谨慎。所有分析结论仅供参考，实际投资决策应基于个人风险承受能力、专业顾问意见及独立判断。
>
> 数据来源为公开网页抓取，可能存在延迟或偏差，不保证准确性。

---

## 七、文件清单

| 文件 | 用途 |
|---|---|
| `index.html` | 工作台主程序（PWA应用） |
| `manifest.json` | APP安装配置（名称、图标、启动方式） |
| `sw.js` | Service Worker（离线缓存） |
| `自动化任务模板.md` | 4套定时任务配置说明 |
| `部署使用指南.md` | 本文档 |

---

## 八、后续优化方向

1. **数据源升级**：接入Tushare（A股）、付费黄金API，提升数据稳定性
2. **云端同步增强**：搭建简单Serverless接收端，实现真正多设备实时同步
3. **电商付费数据**：对接飞瓜/蝉妈妈付费API
4. **推送通知**：定时任务完成后触发App推送提醒
5. **图表增强**：接入ECharts，绘制更丰富的资金流向、金价走势图
6. **生物识别锁**：增加Face ID/指纹解锁，保护财务数据隐私
